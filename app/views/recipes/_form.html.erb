<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>
  <%= f.fields_for :ingredients do |f| %>
    <%= f.label :quantity %>
    <%= f.text_field :quantity %><br>
  <% end %>
  <%= f.submit %>
<% end %>
